#include "VideoSlider.h"
#include <QSlider>
#include <Qt>
#include <QRect>


VideoSlider::VideoSlider(QWidget *parent) : QWidget(parent)
{
    /*
    sliderVideo = new QSlider(parent);
    sliderVideo->setOrientation(Qt::Horizontal);
    sliderVideo->setGeometry(QRect(90, 490, 531, 29));
    sliderVideo->setSizePolicy(QSizePolicy::Maximum, QSizePolicy::Maximum);
    sliderVideo->setMaximum(100);
    sliderVideo->setRange(0, 1000);
    sliderVideo->setPageStep(1);
    sliderVideo->setPageStep(1);

    sliderVideo->show();
*/
/*
    QLabel *p = new QLabel(parent);
    p->setText(QString("i love you!)"));
    p->move(QPoint(200, 540));
    p->show();
*/
}

